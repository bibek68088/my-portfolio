export interface Skill {
  title: string;
  progress: number;
}

export interface Project {
  title: string;
  subtitle: string;
  description: string;
  image: string;
  link: string;
  tag: string;
}

export interface Education {
  title: string;
  sub: string;
  college: string;
  duration: string;
  desc: string;
}

export interface Experience {
  title: string;
  sub: string;
  duration: string;
  desc: string;
}
